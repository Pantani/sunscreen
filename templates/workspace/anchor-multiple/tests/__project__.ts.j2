import * as anchor from "@coral-xyz/anchor";
import { Program } from "@coral-xyz/anchor";
import { {{ program_name | pascal_case }} } from "../target/types/{{ program_name | snake_case }}";

describe("{{ project_name | kebab_case }}", () => {
  // Configure the client to use the local cluster.
  anchor.setProvider(anchor.AnchorProvider.env());

  const program = anchor.workspace.{{ program_name | pascal_case }} as Program<{{ program_name | pascal_case }}>;

  it("loads the program", () => {
    // Scaffold an instruction with `sunscreen scaffold instruction <name> --program {{ program_name | snake_case }}`,
    // then replace this with a real `program.methods.<name>(...).rpc()` call.
    if (!program) throw new Error("program not loaded");
  });
});
